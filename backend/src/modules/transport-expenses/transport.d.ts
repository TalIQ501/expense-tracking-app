export interface TransportType {
  expense_date: string;
  mode: string;
  amount: number;
  origin: string;
  origin_region: string;
  destination: string;
  destination_region: string;
  service_name: string;
  rating: number;
  is_deleted: number;
  recorded_at: string;
  deleted_at: string;
}
